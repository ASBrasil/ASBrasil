import { PrismaClient } from "@prisma/client"; import bcrypt from "bcryptjs"; const db = new PrismaClient(); async function main() { const admin = await db.adminUser.findUnique({ where: { email: "admin@asbrasil.com" } }); console.log("Admin encontrado:", !!admin); if (admin) { console.log("Hash no banco:", admin.passwordHash); console.log("Bate com 'trocar123'?", await bcrypt.compare("trocar123", admin.passwordHash)); } } main().finally(() => db.$disconnect());